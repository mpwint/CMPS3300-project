<!DOCTYPE html>
<html lang="en">
<head>
  <title>New Post Form</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/js/bootstrap.min.js"></script>
</head>
<body>
  
<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field col-xs-12 col-sm-6 col-sm-offset-3">
    <%= form.label :user_id, :class => "col-sm-2 control-label" %>
    <div class = "col-sm-10">
    <%= form.number_field :user_id, :class => "form-control" %>
    </div>
  </div>

  <div class="field col-xs-12 col-sm-6 col-sm-offset-3">
    <%= form.label :poster_email, :class => "col-sm-2 control-label" %>
    <div class = "col-sm-10">
    <%= form.email_field :poster_email, :class => "form-control" %>
    </div>
  </div>

  <div class="field col-xs-12 col-sm-6 col-sm-offset-3">
    <%= form.label :description, :class => "col-sm-2 control-label" %>
    <div class = "col-sm-10">
    <%= form.text_area :description, :class => "form-control" %>
    </div>
  </div>
  
  <style>
    .btn-green {
    color: #fff;
    background-color: #28a745;
    border-color: #28a745
    }
    .btn-red {
    color: #fff;
    background-color: #cb2431;
    border-color: #cb2431
    }
  </style>

  <div class="actions col-xs-12 col-sm-6 col-sm-offset-4">
    <button class="btn btn-sm btn-primary btn-green" type="submit">Create Post</button>
    <button class="btn btn-sm btn-primary btn-red" type="link_to posts_path">Back</button>
  </div>
<% end %>
