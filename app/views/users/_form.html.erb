<!DOCTYPE html>
<html lang="en">
<head>
  <title>New User Form</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/js/bootstrap.min.js"></script>
</head>
<body>
  
<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field col-xs-12 col-sm-6 col-sm-offset-3">
    <%= form.label :name, :class => "col-sm-2 control-label" %>
    <div class = "col-sm-10">
    <%= form.text_field :name, :class => "form-control" %>
    </div>
  </div>

  <div class="field col-xs-12 col-sm-6 col-sm-offset-3">
    <%= form.label :email, :class => "col-sm-2 control-label" %>
    <div class = "col-sm-10">
    <%= form.email_field :email, :class => "form-control" %>
    </div>
  </div>

  <div class="field col-xs-12 col-sm-6 col-sm-offset-3">
    <%= form.label :major, :class => "col-sm-2 control-label" %>
    <div class = "col-sm-10">
    <%= form.text_field :major, :class => "form-control" %>
    </div>
  </div>
  
  <div class="actions col-xs-12 col-sm-6 col-sm-offset-4">
    <%= form.submit nil, :class => "btn btn-sm btn-primary btn-green" %>
  </div>
<% end %>